import Image from 'next/image';
import Container from '../ui/Container';
import GroundCard from '../shared/GroundCard';

const Ground = () => {
  return (
    <div className=''>
      <Container>
        <GroundCard />
      </Container>
    </div>
  );
};

export default Ground;
